```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

```bash
# https://ui.shadcn.com/docs/installation/next
npx shadcn@latest init
npx shadcn@latest add button select input carousel card form dropdown-menu sonner separator badge table drawer
npm i mongoose mongodb zod embla-carousel-autoplay
npm i next-auth@beta bcryptjs react-hook-form @hookform/resolvers @auth/mongodb-adapter
npm i --save-dev @types/bcryptjs
npm i react-medium-image-zoom
npm i zustand
npm i @paypal/react-paypal-js --legasy-peer-deps
npm i query-string
```
